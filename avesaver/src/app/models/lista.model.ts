export interface Lista {
    id?: string;
    nombre: string;
    elementos: string[]; // O el tipo que sea apropiado para los elementos de tu lista
    usuarioId?: string;
    nombreUser?: string;
  }
  